1)  // rewrite zoo using interface 
		public interface Sing{
			public String sing();
		} 
		// done
		// no nned to have body in the interface;

2) //rewrite Bookstore using abstract class Book;
		abstract Book:
		author, title, year, basePrice;
		abstract double price();
		// done
		// couldn't do abstract double price because subclasses use super.price();
		// you could return an instance of subclass when constructing abstract superclass

3) rewrite sorting test case using Jtest 
 	//done. 2 entry points: Jtest and simply assert
 	//also did enums

4) star: implement filter procedure test
	//used compare to sorted collection (by the same predicate) 
	//to make sure that the only the filtered values stay;